{
  "name": "site",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://quangogage.github.io/falcinator/",
  "dependencies": {
    "falcinator": "git+https://github.com/quangogage/falcinator.git#dev",
    "gh-pages": "^1.0.0",
    "react": "^16.1.1",
    "react-dom": "^16.1.1",
    "react-scripts": "1.0.17"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build&&gh-pages -d build",
    "clean": "gh-pages-clean"
  }
}
